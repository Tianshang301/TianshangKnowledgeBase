# 解析数论与密码学

## 一、Dirichlet 级数��?Riemann Zeta 函数

### 1.1 Dirichlet 级数

**Dirichlet 级数**是形如下式的级数��?
$$F(s) = \sum_{n=1}^\infty \frac{a_n}{n^s}$$

其中 $s = \sigma + it$ 为复变量。Dirichlet 级数的收敛性由实部 $\sigma$ 决定，存在收敛横坐标 $\sigma_c$，当 $\sigma > \sigma_c$ 时级数绝对收敛��?
**Dirichlet 卷积与级数的关系**：若 $h = f * g$，则��?
$$\sum_{n=1}^\infty \frac{h(n)}{n^s} = \left( \sum_{n=1}^\infty \frac{f(n)}{n^s} \right) \left( \sum_{n=1}^\infty \frac{g(n)}{n^s} \right)$$

### 1.2 Riemann Zeta 函数定义

Riemann Zeta 函数��?$\operatorname{Re}(s) > 1$ 时定义为 Dirichlet 级数��?
$$\zeta(s) = \sum_{n=1}^\infty \frac{1}{n^s} = 1 + \frac{1}{2^s} + \frac{1}{3^s} + \frac{1}{4^s} + \cdots$$

### 1.3 Euler 乘积公式

��?$\operatorname{Re}(s) > 1$��?\zeta(s)$ 可表示为素数乘积��?
$$\zeta(s) = \prod_{p \text{ 素数}} \left(1 - \frac{1}{p^s}\right)^{-1}$$

这一公式建立��?$\zeta(s)$ 与素数分布之间的深刻联系。从该公式出发可以证明素数有无穷多个��?s \to 1^+$ ��?$\zeta(s) \to \infty$，故右侧乘积发散）��?
### 1.4 特殊��?
| $s$ | $\zeta(s)$ | 数��?|
|-----|-----------|------|
| 2 | $\pi^2 / 6$ | $\approx 1.644934$ |
| 3 | Apéry 常数 | $\approx 1.202057$ |
| 4 | $\pi^4 / 90$ | $\approx 1.082323$ |
| 偶数 | $\zeta(2n) = (-1)^{n+1} \frac{B_{2n} (2\pi)^{2n}}{2(2n)!}$ | 有理数��?$\pi^{2n}$ |

其中 $B_n$ ��?Bernoulli 数：$B_2 = 1/6,\; B_4 = -1/30,\; B_6 = 1/42,\; \ldots$

## 二、Riemann 猜想与解析延��?
### 2.1 解析延拓

$\zeta(s)$ 可解析延拓到整个复平面（$s \neq 1$ 处），满足函数方程：

$$\zeta(s) = 2^s \pi^{s-1} \sin\left(\frac{\pi s}{2}\right) \Gamma(1-s) \zeta(1-s)$$

该函数方程给出了 $\zeta(s)$ 关于直线 $\operatorname{Re}(s) = 1/2$ 的对称性��?
### 2.2 Zeta 函数的零��?
$\zeta(s)$ 的零点分为两类：

1. **平凡零点**��?s = -2, -4, -6, \ldots$（负偶数��?2. **非平凡零��?*：全部位于临界带 $0 < \operatorname{Re}(s) < 1$ ��?
### 2.3 Riemann 猜想

**Riemann 猜想**（Riemann Hypothesis, RH）：$\zeta(s)$ 的所有非平凡零点都位于临界线 $\operatorname{Re}(s) = 1/2$ 上：

$$\zeta(s) = 0 \implies \operatorname{Re}(s) = \frac{1}{2}$$

这是 Clay 数学研究所的七个千禧年难题之一，至今未解决。已知超��?$10^{13}$ 个零点位于临界线上，但尚未完成证明��?
### 2.4 Riemann 猜想与素数分��?
Riemann 猜想等价于素数定理的误差项估计：

$$\pi(x) = \operatorname{Li}(x) + O\!\left(\sqrt{x} \ln x\right)$$

其中 $\operatorname{Li}(x) = \int_2^x \frac{dt}{\ln t}$。若 RH 成立，则素数分布的误差项达到最优上界��?
**显式公式**（Riemann 的贡献）��?
$$\psi(x) = x - \sum_{\rho} \frac{x^\rho}{\rho} - \frac{\zeta'(0)}{\zeta(0)} - \frac{1}{2} \ln\left(1 - \frac{1}{x^2}\right)$$

其中 $\psi(x) = \sum_{p^k \le x} \ln p$ ��?Chebyshev 函数��?\rho$ 遍历 $\zeta(s)$ 的非平凡零点。此公式将素数分布与零点分布直接关联��?
## 三、Dirichlet 定理

### 3.1 定理陈述

**Dirichlet 定理**（Dirichlet's Theorem on Arithmetic Progressions）：��?$\gcd(a, d) = 1$，则算术级数

$$a,\; a + d,\; a + 2d,\; a + 3d,\; \ldots$$

中包含无穷多个素数��?
### 3.2 Dirichlet L-函数

Dirichlet 的证明引入了 **Dirichlet 特征** $\chi$ ��?**Dirichlet L-函数**��?
$$L(s, \chi) = \sum_{n=1}^\infty \frac{\chi(n)}{n^s} = \prod_{p} \left(1 - \frac{\chi(p)}{p^s}\right)^{-1}, \quad \operatorname{Re}(s) > 1$$

Dirichlet 特征 $\chi$ 是模 $d$ 的完全积性函数。对非主特征��?L(1, \chi) \neq 0$ 是证明的关键��?
### 3.3 Dirichlet 密度的推��?
**Dirichlet 密度**（自然密度的一种形式）��?
$$\lim_{s \to 1^+} \frac{\sum_{p \equiv a \pmod{d}} p^{-s}}{\sum_{p} p^{-s}} = \frac{1}{\varphi(d)}$$

这表明素数在互素的剩余类中均匀分布——这��?Chebotarev 密度定理的特例��?
## 四、RSA 密码系统

### 4.1 密钥生成

RSA 密钥生成步骤如下��?
1. 随机选择两个大素��?$p, q$（通常 $\ge 2048$ 位）
2. 计算 $n = pq$
3. 计算 $\varphi(n) = (p-1)(q-1)$
4. 选择公钥指数 $e$，满��?$1 < e < \varphi(n)$ ��?$\gcd(e, \varphi(n)) = 1$
   - 常用值：$e = 65537 = 2^{16} + 1$
5. 计算私钥 $d$，使 $d \equiv e^{-1} \pmod{\varphi(n)}$，即 $ed \equiv 1 \pmod{\varphi(n)}$

**公钥**��?(n, e)$��?*私钥**��?(n, d)$

| 密钥成分 | 选择方式 | 保密��?|
|---------|---------|--------|
| $p, q$ | 随机大素��?| 保密（解密后销毁） |
| $n = pq$ | 计算得到 | 公开 |
| $e$ | 预设��?$65537$ | 公开 |
| $d \equiv e^{-1} \pmod{\varphi(n)}$ | 扩展 Euclid 算法 | 严格保密 |
| $\varphi(n) = (p-1)(q-1)$ | 计算得到 | 严格保密 |

### 4.2 加密与解��?
**加密**：发送方使用接收方的公钥 $(n, e)$ 加密消息 $m$��?0 \le m < n$）：

$$c \equiv m^e \pmod{n}$$

**解密**：接收方使用私钥 $(n, d)$ 解密密文 $c$��?
$$m \equiv c^d \pmod{n}$$

**正��性证��?*：当 $\gcd(m, n) = 1$ 时，��?Euler 定理 $m^{\varphi(n)} \equiv 1 \pmod{n}$，故 $c^d \equiv m^{ed} \equiv m^{k\varphi(n)+1} \equiv m \pmod{n}$。当 $\gcd(m, n) \neq 1$ 时（概率可忽略），可分别��?$p$ ��?$q$ 验证��?
### 4.3 RSA 的安全��?
RSA 的安全性基��?*大整数分解问��?*（IFP）：已知 $n$，分��?$n = pq$ 在计算上不可行��?
**威胁与防��?*��?
| 攻击类型 | 描述 | 防御措施 |
|---------|------|---------|
| 分解 $n$ | 用数域筛法分��?$n$ | $p, q \ge 2^{1024}$ |
| 共模攻击 | 同一 $n$ 不同 $e$ | 不同用户使用不同 $n$ |
| 低指数攻��?| $e$ 太小��?$m$ ��?| 使用 OAEP 填充 |
| 时序攻击 | 通过解密时间推测 $d$ | 使用常数时间算法 |
| 选择密文攻击 | 利用解密预言��?| 使用 OAEP/PS S 填充 |

**实际参数**��?n$ 至少 2048 位，$p, q$ ��?1024 位，$e$ 常用 $65537$，哈希函数配��?OAEP 填充��?
## 五、Diffie-Hellman 密钥交换

### 5.1 协议描述

Diffie-Hellman 密钥交换（DHKE）允许两方在不安全信道上协商共享密钥��?
**协议步骤**��?
1. **公共参数**：选择大素��?$p$ 和模 $p$ 的原��?$g$
2. **Alice** 选择私钥 $a$（随机），计算公��?$A \equiv g^a \pmod{p}$ 并发送给 Bob
3. **Bob** 选择私钥 $b$（随机），计算公��?$B \equiv g^b \pmod{p}$ 并发送给 Alice
4. **Alice** 计算共享密钥 $K \equiv B^a \equiv g^{ab} \pmod{p}$
5. **Bob** 计算共享密钥 $K \equiv A^b \equiv g^{ab} \pmod{p}$

$$
\begin{aligned}
\text{Alice} &\xrightarrow{A = g^a \bmod p} \text{Bob} \\
\text{Alice} &\xleftarrow{B = g^b \bmod p} \text{Bob} \\
K &= g^{ab} \bmod p
\end{aligned}
$$

### 5.2 安全��?
DH 的安全性基��?*计算 Diffie-Hellman 问题**（CDH）：已知 $g, g^a, g^b$，求 $g^{ab}$。CDH 的困难性等价于**离散对数问题**（DLP）的困难性��?
**中间人攻��?*：若未认证身份，攻击��?Mallory 可以截获并替��?$A$ ��?$B$，分别与 Alice ��?Bob 建立共享密钥。防御措施是使用数字签名或证书认证公钥��?
## 六、椭圆曲线密码学

### 6.1 椭圆曲线定义

椭圆曲线 $E$ 在域 $K$ 上定义为 Weierstrass 方程��?
$$E: y^2 = x^3 + ax + b, \quad a, b \in K,\; 4a^3 + 27b^2 \neq 0$$

非奇异性条��?$4a^3 + 27b^2 \neq 0$ 确保曲线无奇点（尖点或自交点）��?
### 6.2 椭圆曲线上的群运��?
椭圆曲线上的点（包括无穷远点 $\mathcal{O}$）构��?Abel 群：

**加法规则**��?- $\mathcal{O}$ 为单位元��?P + \mathcal{O} = \mathcal{O} + P = P$
- 逆元��?P = (x, y)$ 的逆为 $-P = (x, -y)$
- 点加：设 $P = (x_1, y_1)$��?Q = (x_2, y_2)$��?P \neq \pm Q$，则 $P + Q = (x_3, y_3)$��?
  $$x_3 = \lambda^2 - x_1 - x_2,\quad y_3 = \lambda(x_1 - x_3) - y_1$$
  $$\lambda = \frac{y_2 - y_1}{x_2 - x_1} \quad (P \neq Q),\qquad 
  \lambda = \frac{3x_1^2 + a}{2y_1} \quad (P = Q)$$

### 6.3 ECDLP ��?ECC

**椭圆曲线离散对数问题**（ECDLP）：给定椭圆曲线上的��?$P$ ��?$Q = kP$，求整数 $k$��?
ECC 的主要优势是**密钥长度更小**��?
| 安全等级（位��?| RSA 密钥长度 | ECC 密钥长度 |
|--------------|-------------|-------------|
| 80 | 1024 | 160 |
| 112 | 2048 | 224 |
| 128 | 3072 | 256 |
| 192 | 7680 | 384 |
| 256 | 15360 | 512 |

**ECC 应用**��?- ECDH（椭圆曲��?Diffie-Hellman）密钥交��?- ECDSA（椭圆曲线数字签名算法）
- Ed25519 签名方案
- SM2 中国国家密码标准

## 七、素性检��?
### 7.1 Miller-Rabin 素性测��?
Miller-Rabin 测试是实践中使用最广泛的概率素性测试��?
**算法原理**：将 $n-1$ 写为 $n-1 = 2^s \cdot d$，其��?$d$ 为奇数。若 $n$ 为素数，则对任意 $a$��?\gcd(a, n) = 1$），必有��?
- $a^d \equiv 1 \pmod{n}$，或��?- 存在 $0 \le r < s$ 使得 $a^{2^r d} \equiv -1 \pmod{n}$

**算法步骤**��?1. ��?$n < 2$ ��?$n$ 为偶数，返回合数
2. 分解 $n-1 = 2^s \cdot d$��?d$ 为奇数）
3. 随机选择底数 $a \in [2, n-2]$
4. 计算 $x = a^d \bmod n$
5. ��?$x \equiv 1$ ��?$x \equiv n-1$，通过本轮测试
6. 重复 $r = 1$ ��?$s-1$��?x = x^2 \bmod n$，若 $x \equiv n-1$，通过
7. 否则判定合数

**错误概率**：单次测试将合数判为素数的概率不超过 $1/4$。重��?$t$ 次后，错误率降至 $4^{-t}$。使��?$t = 64$ 的错误率可忽略不计��?
### 7.2 确定��?Miller-Rabin

��?$n$ 范围已知时，可使用��定性底数集��?
| $n$ 范围 | 底数��?|
|----------|--------|
| $n < 2^{32}$ | $\{2, 7, 61\}$ |
| $n < 2^{64}$ | $\{2, 325, 9375, 28178, 450775, 9780504, 1795265022\}$ |
| $n < 3{,}317{,}044{,}064{,}679{,}887{,}385{,}961{,}981$ | $\{2, 3, 5, 7, 11, 13, 17\}$ |

### 7.3 其他素性检��?
| 方法 | 类型 | 说明 |
|------|------|------|
| 试除��?| 确定��?| $O(\sqrt{n})$，仅用于小整��?|
| Fermat 测试 | 概率 | 存在 Carmichael 数导致假阴��?|
| Solovay-Strassen | 概率 | 利用 Euler 准则 |
| Miller-Rabin | 概率 | 实践中首��?|
| AKS | 确定��?| 首个多项式时间��定性算法（$O(\log^{6+\varepsilon} n)$��?|
| Lucas-Lehmer | 确定��?| 专用��?Mersenne 素数 |
| ECPP | 确定��?| 基于椭圆曲线，可验证大素��?|

## 八、整数分解算��?
### 8.1 试除法与 Pollard's Rho

**试除��?*：从小素数开始试除，复杂��?$O(\sqrt{n})$，仅适用于小整数��?
**Pollard's Rho 算法**：基��?Floyd 判圈算法和碰撞检测��?
$$f(x) = (x^2 + c) \bmod n$$

核心思想：若 $d = \gcd(|x_i - x_{2i}|, n) > 1$，则找到非平凡因��?$d$。期望复杂度 $O(n^{1/4})$��?
### 8.2 其他分解算法

| 算法 | 时间复杂��?| 适用场景 |
|------|-----------|---------|
| Pollard's Rho | $O(n^{1/4})$ | 中小整数，有小平因子 |
| Pollard $p-1$ | $O(B \log n)$ | $p-1$ 为光滑数 |
| Williams $p+1$ | $O(B \log n)$ | $p+1$ 为光滑数 |
| 连分数法 (CFRAC) | $L_n[1/2, \sqrt{2}]$ | 历史意义 |
| 二次筛法 (QS) | $L_n[1/2, 1]$ | $n < 10^{100}$ |
| 数域筛法 (NFS) | $L_n[1/3, (64/9)^{1/3}]$ | 大整数通用最��?|

其中 $L_n[\alpha, c] = \exp\left((c + o(1)) (\ln n)^\alpha (\ln \ln n)^{1-\alpha}\right)$��?
**RSA 分解挑战**��?- RSA-129��?26 位）��?994 年用 QS 分解耗时 8 个月
- RSA-240��?95 位）��?019 年用 NFS 分解
- RSA-250��?29 位）��?020 年分��?- 当前推荐 RSA 密钥长度��?048 位以��?
## 九、数论密码学的应��?
### 9.1 公钥密码体系总览

| 密码系统 | 困难问题 | 密钥类型 | 典型应用 |
|---------|---------|---------|---------|
| RSA | 大整数分��?| 公钥加密/签名 | SSL/TLS, PGP |
| ElGamal | DLP | 公钥加密 | GPG, 混合加密 |
| DSA/ECDSA | DLP/ECDLP | 数字签名 | 数字证书, 区块��?|
| DH/ECDH | CDH | 密钥交换 | TLS 握手 |
| Paillier | 合数剩余 | 同态加��?| 隐私计算 |
| Cocks | 二次剩余 | 公钥加密 | 数论教学示例 |
| NTRU | 格问��?| 后量子密��?| 抗量子计��?|

### 9.2 实际应用场景

**SSL/TLS 协议**：在 TLS 1.3 中，密钥交换使用 ECDHE（基��?ECDH 的临时密钥交换），身份认证使��?ECDSA ��?RSA 签名��?
**区块链与加密货币**��?- Bitcoin：ECDSA 签名（secp256k1 曲线��?- Ethereum：ECDSA 签名，近期支持基于配对的密码��?- 零知识证明（zk-SNARKs）：利用椭圆曲线上的双线性配��?
**同态加密与隐私计算**��?- Paillier 加密：支持加法同态，可用于电子投��?- BGV/BFV 方案：全同态加密，基于格密码（后量子安全）

### 9.3 后量子密码学

量子计算机可有效破解目前主流的公钥密码（Shor 算法可解决分解和离散对数问题）��?*后量子密码学**（PQC）的研究方向包括��?
- **基于格的密码**：NTRU, Kyber, Dilithium
- **基于编码的密��?*：McEliece, Classic McEliece
- **基于多变量多项式的密��?*��?Rainbow, GeMSS
- **基于哈希的签��?*：SPHINCS+
- **基于同源的密��?*：SIKE（已被攻击）

NIST PQC 标准��?024）已选定 CRYSTALS-Kyber（KEM）和 CRYSTALS-Dilithium（签名）作为最终标准��?
---

**参考教��?*：Hardy & Wright《An Introduction to the Theory of Numbers》、Apostol《Introduction to Analytic Number Theory》、Katz & Lindell《Introduction to Modern Cryptography》、Menezes《Handbook of Applied Cryptography��?

## 相关条目

[[02_NaturalSciences/Mathematics/Algebra/INDEX|Algebra]], Cryptography, [[DiscreteMathematics]], [[02_NaturalSciences/Mathematics/MathematicalAnalysis/INDEX|MathematicalAnalysis]]
