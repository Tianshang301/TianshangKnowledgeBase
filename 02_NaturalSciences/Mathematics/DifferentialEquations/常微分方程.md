# 常微分方��?(Ordinary Differential Equations)

## 一、基本概��?
### 1.1 微分方程的定��?
含有未知函数及其导数的方程称��?*微分方程**。若未知函数仅依赖于一个自变量，则称为**常微分方��?(ODE)**��?
一般形式：
$$\frac{dy}{dx} = f(x, y)$$

其中 $y = y(x)$ 是未知函数，$f$ 是已知函数��?
### 1.2 阶数与线性��?
方程中出现的最高阶导数的阶数称为微分方程的**阶数**��?
**线��?ODE** 的标准形式：
$$a_n(x) y^{(n)} + a_{n-1}(x) y^{(n-1)} + \cdots + a_1(x) y' + a_0(x) y = g(x)$$

��?$g(x) = 0$，则称为**齐次**方程；否则为**非齐��?*方程��?
### 1.3 解的概念

满足微分方程的函��?$y = \phi(x)$ 称为**��?*。含任意常数的解称为**通解**；给定了初始条件 $y(x_0) = y_0$ 的解称为**特解**��?
## 二、方向场与几何意��?
一阶方��?$dy/dx = f(x, y)$ 在每��?$(x, y)$ 确定了切线的斜率。在平面上绘制短线段表示方向场，可直观观察解的走向��?
等斜线是方向场中斜率相同的点的轨迹：
$$f(x, y) = k \quad (k \text{ 为常数})$$

## 三、解的存在性与唯一��?
### 3.1 Picard–Lindelöf 定理

��?$f(x, y)$ 在矩形区��?$R = \{ (x,y) : |x-x_0| \leq a, |y-y_0| \leq b \}$ 上连续且关于 $y$ 满足 Lipschitz 条件，则初值问��?$$\frac{dy}{dx} = f(x,y), \quad y(x_0) = y_0$$
在某个区��?$|x - x_0| \leq h$ 上存在唯一解��?
### 3.2 Lipschitz 条件

存在常数 $L > 0$，使得对所��?$(x, y_1), (x, y_2) \in R$ 有：
$$|f(x, y_1) - f(x, y_2)| \leq L |y_1 - y_2|$$

## 四、可分离变量方程

### 4.1 标准形式

$$\frac{dy}{dx} = g(x) h(y)$$

### 4.2 解法

将变量分离后两边积分��?$$\int \frac{dy}{h(y)} = \int g(x) \, dx + C$$

��?$h(y) = 0$ 有解，则得到平凡解��?
**例：** $\frac{dy}{dx} = xy$
$$\int \frac{dy}{y} = \int x \, dx \implies \ln|y| = \frac{x^2}{2} + C \implies y = Ce^{x^2/2}$$

## 五、恰当方��?(Exact Equations)

### 5.1 形式与条��?
将一阶方程写为微分形式：
$$M(x, y) \, dx + N(x, y) \, dy = 0$$

若存在函��?$F(x, y)$ 使得 $dF = M dx + N dy$，则方程��?*恰当方程**。充要条件：
$$\frac{\partial M}{\partial y} = \frac{\partial N}{\partial x}$$

### 5.2 解法

求解 $F(x, y) = c$，其中：
$$\frac{\partial F}{\partial x} = M, \quad \frac{\partial F}{\partial y} = N$$

先对 $x$ 积分��?$$F(x, y) = \int M(x, y) \, dx + \phi(y)$$

再代��?$\partial F/\partial y = N$ 确定 $\phi(y)$��?
### 5.3 积分因子

若非恰当，可寻找 $\mu(x, y)$ 使得 $\mu M dx + \mu N dy = 0$ 成为恰当方程��?
常见积分因子形式��?- ��?$\frac{M_y - N_x}{N}$ 仅为 $x$ 的函数，��?$\mu = e^{\int \frac{M_y - N_x}{N} dx}$
- ��?$\frac{N_x - M_y}{M}$ 仅为 $y$ 的函数，��?$\mu = e^{\int \frac{N_x - M_y}{M} dy}$

## 六、一阶线性方��?
### 6.1 标准形式

$$\frac{dy}{dx} + P(x) y = Q(x)$$

### 6.2 积分因子��?
积分因子��?$$\mu(x) = e^{\int P(x) \, dx}$$

两边乘以 $\mu(x)$��?$$\frac{d}{dx} \left( \mu(x) y \right) = \mu(x) Q(x)$$

通解��?$$y = \frac{1}{\mu(x)} \int \mu(x) Q(x) \, dx + \frac{C}{\mu(x)}$$

### 6.3 例子

��?$y' + 2xy = x$��?- $P(x) = 2x$��?\mu = e^{\int 2x dx} = e^{x^2}$
- $y = e^{-x^2} \int x e^{x^2} dx + Ce^{-x^2} = \frac{1}{2} + Ce^{-x^2}$

## 七、Bernoulli 方程

### 7.1 形式

$$\frac{dy}{dx} + P(x) y = Q(x) y^n, \quad n \neq 0, 1$$

### 7.2 解法

��?$v = y^{1-n}$，则方程化为线性方程：
$$\frac{dv}{dx} + (1-n) P(x) v = (1-n) Q(x)$$

解出 $v$ 后回代得 $y$��?
**例：** $y' + y = xy^2$��?n=2$��?- ��?$v = y^{-1}$，则 $v' - v = -x$
- 解得 $v = -x - 1 + Ce^x$，故 $y = 1/(-x-1+Ce^x)$

## 八、齐次方��?(Homogeneous Equations)

### 8.1 定义

��?$f(x, y)$ 可写��?$f(x, y) = \phi(y/x)$，则称方程为**齐次**的��?
### 8.2 解法

��?$v = y/x$，即 $y = vx$，则��?$$\frac{dy}{dx} = v + x \frac{dv}{dx}$$

代入得：
$$v + x \frac{dv}{dx} = \phi(v) \implies x \frac{dv}{dx} = \phi(v) - v$$

分离变量求解��?
## 九、二阶线性齐次方程（常系数）

### 9.1 标准形式

$$a y'' + b y' + c y = 0, \quad a, b, c \in \mathbb{R}$$

### 9.2 特征方程��?
��?$y = e^{rx}$，代入得特征方程��?$$a r^2 + b r + c = 0$$

判别��?$\Delta = b^2 - 4ac$��?
**(1) 两个不同实根 $r_1 \neq r_2$��?*
$$y = C_1 e^{r_1 x} + C_2 e^{r_2 x}$$

**(2) 重根 $r_1 = r_2 = r$��?*
$$y = (C_1 + C_2 x) e^{rx}$$

**(3) 共轭复根 $r = \alpha \pm i\beta$��?*
$$y = e^{\alpha x} (C_1 \cos \beta x + C_2 \sin \beta x)$$

## 十、待定系数法

用于求解非齐次方��?$ay'' + by' + cy = g(x)$��?
### 10.1 方法

设特解形式与 $g(x)$ 相似，代入��定系数：

| $g(x)$ 形式 | 特解形式 |
|---|---|
| $P_n(x)$ | $x^s Q_n(x)$ |
| $A e^{kx}$ | $x^s B e^{kx}$ |
| $A \cos \omega x + B \sin \omega x$ | $x^s (C \cos \omega x + D \sin \omega x)$ |

其中 $s$ 是避免与齐次解冲突的调整因子��?s=0,1,2$）��?
## 十一、参数变易法 (Variation of Parameters)

### 11.1 方法

已知齐次方程的基本解 $y_1, y_2$，设特解��?$$y_p = u_1(x) y_1(x) + u_2(x) y_2(x)$$

### 11.2 公式

$$\begin{cases}
u_1' y_1 + u_2' y_2 = 0 \\[4pt]
u_1' y_1' + u_2' y_2' = g(x)/a
\end{cases}$$

解得��?$$u_1' = -\frac{y_2 g}{a W}, \quad u_2' = \frac{y_1 g}{a W}$$

其中 $W = y_1 y_2' - y_2 y_1'$ ��?Wronskian��?
## 十二、Wronskian

### 12.1 定义

函数 $y_1, y_2, \ldots, y_n$ ��?Wronskian 为：
$$W(y_1, \ldots, y_n) = \det
\begin{pmatrix}
y_1 & y_2 & \cdots & y_n \\
y_1' & y_2' & \cdots & y_n' \\
\vdots & \vdots & \ddots & \vdots \\
y_1^{(n-1)} & y_2^{(n-1)} & \cdots & y_n^{(n-1)}
\end{pmatrix}$$

### 12.2 性质

- ��?$W \neq 0$，则函数线性无关��?- 对于二阶 ODE $y'' + p(x)y' + q(x)y = 0$，有 Abel 定理��?$$W(x) = W(x_0) \exp\left(-\int_{x_0}^x p(t) dt\right)$$

## 相关条目

[[02_NaturalSciences/Mathematics/MathematicalAnalysis/INDEX|MathematicalAnalysis]], [[01_K12/JuniorHigh/Physics/INDEX|Physics]], [[02_NaturalSciences/Mathematics/ComputationalMathematics/INDEX|ComputationalMathematics]], PartialDifferentialEquations
