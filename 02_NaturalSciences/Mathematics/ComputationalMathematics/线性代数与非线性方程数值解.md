# 线性代数与非线性方程数值解

## 一、高斯消元与 LU 分解

### 1.1 高斯消元��?
高斯消元法由前向消元和回代求解两个阶段组成��?
**前向消元**：将增广矩阵 $[A \mid b]$ 化为上三角形式。对��?$k = 1, 2, \dots, n-1$��?
$$
m_{ik} = \frac{a_{ik}^{(k)}}{a_{kk}^{(k)}}, \quad i = k+1, \dots, n
$$

$$
a_{ij}^{(k+1)} = a_{ij}^{(k)} - m_{ik} a_{kj}^{(k)}, \quad j = k+1, \dots, n
$$

$$
b_i^{(k+1)} = b_i^{(k)} - m_{ik} b_k^{(k)}
$$

**回代求解**��?
$$
x_n = \frac{b_n^{(n)}}{a_{nn}^{(n)}}
$$

$$
x_i = \frac{b_i^{(i)} - \sum_{j=i+1}^n a_{ij}^{(i)} x_j}{a_{ii}^{(i)}}, \quad i = n-1, \dots, 1
$$

### 1.2 LU 分解

将矩��?$A$ 分解为下三角矩阵 $L$ 和上三角矩阵 $U$ 的乘积：

$$
A = LU, \quad L = \begin{pmatrix}
1 & 0 & \dots & 0 \\
l_{21} & 1 & \dots & 0 \\
\vdots & \vdots & \ddots & \vdots \\
l_{n1} & l_{n2} & \dots & 1
\end{pmatrix}, \quad U = \begin{pmatrix}
u_{11} & u_{12} & \dots & u_{1n} \\
0 & u_{22} & \dots & u_{2n} \\
\vdots & \vdots & \ddots & \vdots \\
0 & 0 & \dots & u_{nn}
\end{pmatrix}
$$

求解过程��?
1. 前代：解 $Ly = b$��?y_1 = b_1$��?y_i = b_i - \sum_{j=1}^{i-1} l_{ij} y_j$
2. 回代：解 $Ux = y$��?x_n = y_n / u_{nn}$��?x_i = (y_i - \sum_{j=i+1}^n u_{ij} x_j) / u_{ii}$

LU 分解的计算量��?$O(2n^3/3)$，与高斯消元相同，但优势在于可高效处理多个右端项��?
### 1.3 Doolittle 分解��?Crout 分解

Doolittle 分解��?L$ 为单位下三角��?U$ 为上三角（上述标准形式）��?
Crout 分解��?L$ 为下三角��?U$ 为单位上三角��?
## 二、乔列斯基分��?
### 2.1 对称正定矩阵��?Cholesky 分解

��?$A \in \mathbb{R}^{n \times n}$ 对称正定（SPD），则存在唯一的下三角矩阵 $L$ 使得��?
$$
A = LL^T
$$

其中 $L$ 的对角元为正数。计算公式：

$$
l_{jj} = \sqrt{a_{jj} - \sum_{k=1}^{j-1} l_{jk}^2}
$$

$$
l_{ij} = \frac{a_{ij} - \sum_{k=1}^{j-1} l_{ik} l_{jk}}{l_{jj}}, \quad i > j
$$

### 2.2 乔列斯基分解的特��?
- 计算量约��?$O(n^3/3)$，是 LU 分解的一��?- 无需选主元（对称正定矩阵保证数值稳定）
- 存储量小，只需存储 $L$ ��?$n(n+1)/2$ 个元��?
### 2.3 LDL^T 分解

为避免开平方运算，可使用 $LDL^T$ 分解��?
$$
A = LDL^T
$$

其中 $L$ 为单位下三角阵，$D = \text{diag}(d_1, d_2, \dots, d_n)$ 为对角阵��?
## 三、选主元策��?
### 3.1 为何需要选主��?
��?$a_{kk}$ 接近零或很小时，直接消元会导致：
- 除以小主元放大舍入误��?- 数值不稳定��?
### 3.2 部分选主元（列主元）

在第 $k$ 步，从第 $k$ 列的��?$k$ 行到��?$n$ 行中选取绝对值最大的元素作为主元��?
$$
|a_{p,k}| = \max_{k \leq i \leq n} |a_{i,k}|
$$

交换��?$k$ 行和��?$p$ 行后进行消元��?
### 3.3 全选主��?
在所有剩余元��?$a_{ij}$��?i,j \geq k$）中选取绝对值最大的元素作为主元，需要同时交换行和列。全选主元数值稳定性最好，但交换列会改变变量顺序，实现较复杂，实际中较少使用��?
### 3.4 选主元策略对��?
| 策略 | 搜索范围 | 数值稳定��?| 实现复杂��?| 计算开销 |
|------|---------|-----------|-----------|---------|
| 不选主��?| ��?| ��?| 最简��?| ��?|
| 部分选主��?| 当前��?| 较好 | 简��?| $O(n^2)$ |
| 全选主��?| 所有剩余元��?| 最��?| 复杂 | $O(n^3)$ |
| 比例选主��?| 缩放后的��?| 较好 | 中等 | $O(n^2)$ |

## 四、雅可比迭代与高��?赛德尔迭��?
### 4.1 雅可比迭��?
��?$Ax = b$ 的第 $i$ 个方程解��?$x_i$��?
$$
x_i^{(k+1)} = \frac{1}{a_{ii}} \left( b_i - \sum_{j \neq i} a_{ij} x_j^{(k)} \right), \quad i = 1, 2, \dots, n
$$

矩阵形式��?
$$
x^{(k+1)} = D^{-1}(b - (L + U)x^{(k)})
$$

其中 $D$ ��?$A$ 的对角部分，$L$ ��?$U$ 分别为严格下三角和严格上三角部分��?
**收敛条件**：若 $A$ 严格对角占优��?|a_{ii}| > \sum_{j \neq i} |a_{ij}|$），��?Jacobi 迭代收敛��?
### 4.2 高斯-赛德尔迭��?
��?Jacobi 不同，Gauss-Seidel 在计��?$x_i^{(k+1)}$ 时立即使用已更新的分量：

$$
x_i^{(k+1)} = \frac{1}{a_{ii}} \left( b_i - \sum_{j=1}^{i-1} a_{ij} x_j^{(k+1)} - \sum_{j=i+1}^n a_{ij} x_j^{(k)} \right)
$$

矩阵形式��?
$$
x^{(k+1)} = (D + L)^{-1}(b - Ux^{(k)})
$$

### 4.3 收敛性分��?
迭代法收敛的充要条件是迭代矩阵的谱半��?$\rho(G) < 1$��?
| 方法 | 迭代矩阵 $G$ | 收敛速度 | 存储要求 |
|------|-------------|---------|---------|
| Jacobi | $D^{-1}(L + U)$ | 较慢 | 需同时存储 $x^{(k)}$ ��?$x^{(k+1)}$ |
| Gauss-Seidel | $(D + L)^{-1}U$ | 通常��?Jacobi 快约 2 ��?| 可原地更��?|

### 4.4 对角占优与收敛��?
| 矩阵性质 | Jacobi | Gauss-Seidel |
|---------|--------|-------------|
| 严格对角占优 | 收敛 | 收敛 |
| 对称正定 | 不一��?| 收敛 |
| 不可约对角占��?| 收敛 | 收敛 |

## 五、SOR 与共轭梯度法

### 5.1 逐次超松弛迭代（SOR��?
SOR ��?Gauss-Seidel 的基础上引入松弛因��?$\omega$��?
$$
x_i^{(k+1)} = (1 - \omega) x_i^{(k)} + \frac{\omega}{a_{ii}} \left( b_i - \sum_{j=1}^{i-1} a_{ij} x_j^{(k+1)} - \sum_{j=i+1}^n a_{ij} x_j^{(k)} \right)
$$

矩阵形式��?
$$
x^{(k+1)} = (D + \omega L)^{-1}[(1 - \omega)D - \omega U]x^{(k)} + \omega (D + \omega L)^{-1} b
$$

### 5.2 最优松弛因��?
对于对称正定矩阵且具有相容次序的性质，最优松弛因子为��?
$$
\omega_{\text{opt}} = \frac{2}{1 + \sqrt{1 - \rho(J)^2}}
$$

其中 $\rho(J)$ ��?Jacobi 迭代矩阵的谱半径。当 $A$ 为对称正定三对角矩阵时，$0 < \omega < 2$ 保证收敛��?\omega > 1$ 为超松弛��?\omega < 1$ 为低松弛��?
### 5.3 共轭梯度��?
共轭梯度法（CG）用于求��?$Ax = b$ 其中 $A$ 对称正定。算法：

$$
\begin{aligned}
r_0 &= b - Ax_0, \quad p_0 = r_0 \\
\alpha_k &= \frac{r_k^T r_k}{p_k^T A p_k} \\
x_{k+1} &= x_k + \alpha_k p_k \\
r_{k+1} &= r_k - \alpha_k A p_k \\
\beta_k &= \frac{r_{k+1}^T r_{k+1}}{r_k^T r_k} \\
p_{k+1} &= r_{k+1} + \beta_k p_k
\end{aligned}
$$

### 5.4 预处��?
预处理共轭梯度法（PCG）通过引入预处理矩��?$M$ 加速收敛：

$$
M^{-1} A x = M^{-1} b
$$

理想情况��?$M \approx A^{-1}$。常用预处理方法：对角预处理（Jacobi）、不完全 Cholesky 分解（ICCG）、SSOR 预处理��?
### 5.5 迭代法对��?
| 方法 | 适用矩阵 | 每步复杂��?| 收敛速度 | 需参数 |
|------|---------|-----------|---------|-------|
| Jacobi | 对角占优 | $O(n^2)$ | 线性，��?| ��?|
| Gauss-Seidel | 各种 | $O(n^2)$ | 线性，��?| ��?|
| SOR | 相容次序 SPD | $O(n^2)$ | 线性，可调 | $\omega$ |
| CG | SPD | $O(n^2)$ | 超线��?| ��?|
| PCG | SPD | $O(n^2)$ | ��?| $M$ |

## 六、非线性方程求��?
### 6.1 二分��?
��?$f \in C[a,b]$ ��?$f(a) f(b) < 0$，则 $f$ ��?$(a,b)$ 内至少有一个根。二分法每次取中��?$c = (a+b)/2$，根��?$f(c)$ 的符号缩小区间��?
误差估计��?
$$
|x_n - \alpha| \leq \frac{b - a}{2^n}
$$

二分法收敛速度较慢（线性收敛），但稳健可靠，总能收敛��?
### 6.2 牛顿��?
牛顿法利用函数的一阶泰勒展开��?
$$
x_{n+1} = x_n - \frac{f(x_n)}{f'(x_n)}
$$

**几何意义**：在 $(x_n, f(x_n))$ 处作切线，与 $x$ 轴的交点作为下一个近似��?
**收敛��?*：若 $f \in C^2$ ��?$f'(\alpha) \neq 0$，则��?$x_0$ 充分接近 $\alpha$ 时，牛顿法具有二阶（平方）收敛：

$$
|x_{n+1} - \alpha| \leq C |x_n - \alpha|^2
$$

**缺点**：需要计算导数，且对初值敏感��?
### 6.3 简化牛顿法

为避免每次迭代计算导数，可采用：

$$
x_{n+1} = x_n - \frac{f(x_n)}{f'(x_0)}
$$

仅需 $x_0$ 处的导数，但收敛速度降为线性��?
### 6.4 割线��?
用差商近似导数：

$$
x_{n+1} = x_n - \frac{f(x_n)(x_n - x_{n-1})}{f(x_n) - f(x_{n-1})}
$$

**收敛��?*��?\rho = (1 + \sqrt{5})/2 \approx 1.618$（黄金分割比），属于超线性收敛��?
**特点**：无需计算导数，每步只需一次函数求值，但需要两个初始点��?
### 6.5 不动点迭��?
��?$f(x) = 0$ 改写��?$x = \varphi(x)$，构造迭代：

$$
x_{n+1} = \varphi(x_n)
$$

**收敛定理**：若 $\varphi \in C[a,b]$��?|\varphi'(x)| \leq L < 1$ 对所��?$x \in [a,b]$ 成立，则迭代收敛到唯一不动点，且收敛阶��?$|\varphi'(\alpha)|$��?
### 6.6 方法对比

| 方法 | 收敛��?| 每步函数求��?| 导数需��?| 初始值需��?| 稳健��?|
|------|-------|------------|---------|-----------|-------|
| 二分��?| 线性（$C = 1/2$��?| 1 | 不需��?| $f(a)f(b) < 0$ 区间 | 最��?|
| 牛顿��?| 二阶��?p = 2$��?| $f + f'$ | 需��?| 靠近真根 | ��?|
| 割线��?| 超线性（$p \approx 1.618$��?| 1 | 不需��?| 两个靠近真根的点 | ��?|
| 不动点迭��?| 线性（$p = 1$��?| 1 | 需��?$\varphi'$ | 靠近不动��?| 依赖��?$\varphi$ |

### 6.7 牛顿法求解非线性方程组

对于 $F(x) = 0$��?F: \mathbb{R}^n \to \mathbb{R}^n$��?
$$
x^{(k+1)} = x^{(k)} - J(x^{(k)})^{-1} F(x^{(k)})
$$

其中 $J(x^{(k)})$ ��?$F$ ��?$x^{(k)}$ 处的雅可比矩阵：

$$
J_{ij} = \frac{\partial F_i}{\partial x_j}
$$

实际计算中，不直接求逆，而是求解线性方程组��?
$$
J(x^{(k)}) \, \Delta x^{(k)} = -F(x^{(k)})
$$

$$
x^{(k+1)} = x^{(k)} + \Delta x^{(k)}
$$

拟牛顿法（如 Broyden 方法）用低秩更新近似雅可比矩阵，避免每次迭代计算导数��?
---

**参考教��?*：Burden & Faires《Numerical Analysis》、李庆扬《数值分析》、Saad《Iterative Methods for Sparse Linear Systems��?

## 相关条目

[[02_NaturalSciences/Mathematics/Algebra/INDEX|Algebra]], [[02_NaturalSciences/Mathematics/MathematicalAnalysis/INDEX|MathematicalAnalysis]], ComputerScience, NumericalAnalysis
